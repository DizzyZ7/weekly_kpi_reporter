# 📊 Weekly KPI Reporter  
_Автоматизация еженедельного отчёта по метрикам_

Этот проект автоматически формирует **еженедельный KPI-отчёт** на основе данных из CSV-файлов:

- считает ключевые метрики (новые пользователи, выручка, конверсия, средний чек)
- строит Excel-отчёт с несколькими листами
- опционально отправляет краткое резюме и файл отчёта в **Telegram**

Проект имитирует типичный сценарий в компании: раз в неделю нужно собрать данные из систем (CRM/платежи), агрегировать и разослать руководству.

---

## 🚀 Возможности

- Загрузка данных пользователей и платежей из CSV
- Автоматическое определение отчётного периода (последняя неделя по данным)
- Расчёт KPI по дням и за период:
  - новые пользователи
  - платящие пользователи
  - количество платежей
  - выручка
  - конверсия (оплатившие / новые)
  - средний чек
- Формирование Excel-отчёта с листами:
  - `raw_users`
  - `raw_payments`
  - `daily_kpi`
  - `summary`
- (Опционально) отправка итогов и отчёта в Telegram (через Telegram Bot API)

---

## 🛠 Стек технологий

- **Python 3.x**
- `pandas` — работа с данными и аналитика
- `openpyxl` — сохранение в Excel
- `matplotlib` — можно использовать для графиков (заложено в зависимости)
- `requests` — отправка отчёта в Telegram

---

## 📁 Структура проекта

```bash
weekly_kpi_reporter/
├─ data_sample/
│  ├─ users_sample.csv      # пример данных по пользователям
│  └─ payments_sample.csv   # пример данных по платежам
├─ config.py                # пути по умолчанию и настройки Telegram
├─ loader.py                # загрузка CSV-файлов
├─ transform.py             # расчёт KPI и агрегированных итогов
├─ report_builder.py        # генерация Excel-отчёта
├─ notifier.py              # отправка отчёта в Telegram
├─ main.py                  # точка входа (CLI)
├─ requirements.txt         # зависимости
└─ .gitignore               # игнор для окружения, данных и отчётов
